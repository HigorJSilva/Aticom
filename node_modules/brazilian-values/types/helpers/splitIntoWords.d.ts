/**
 * Normalizes text white spaces, then splits into words.
 * @param value - A `string` (text) to be splitted into words.
 */
declare const splitIntoWords: (value: string) => string[];
export default splitIntoWords;
